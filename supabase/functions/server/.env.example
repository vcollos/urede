# Variáveis de ambiente para rodar a Edge Function localmente via Deno
# Copie para supabase/functions/server/.env e preencha com os valores do seu projeto

SUPABASE_URL=https://<project-ref>.supabase.co
SERVICE_ROLE_KEY=<service-role-key>
SUPABASE_ANON_KEY=<anon-key>
PORT=8000

# CORS: defina a(s) origem(ns) permitida(s). Suporta múltiplas separadas por vírgula e wildcard do tipo *.dominio.com
ALLOWED_ORIGINS=https://urede.deno.com,https://urede.vercel.app

# Banco de dados
# Schema (padrão 'public') e prefixo de tabelas (ex.: 'urede_' para usar 'urede_cooperativas', 'urede_cidades', etc.)
DB_SCHEMA=public
TABLE_PREFIX=urede_
